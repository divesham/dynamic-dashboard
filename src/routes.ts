export const routes = {
    home: "/",
    reports: "/reports",
    groupedReports: "/grouped-reports", // Example route for GroupedReports
  };
  
  // Fix: The `export` statement ensures this file is treated as a module.
  